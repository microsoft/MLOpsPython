steps:
  - task: Docker@2
    displayName: Build and Push
    inputs:
      command: buildAndPush
      containerRegistry: '$(containerRegistry)'
      repository: '$(imageName)'
      tags: 'latest'
      buildContext: '$(Build.SourcesDirectory)/Code' 
      dockerFile: '$(Build.SourcesDirectory)/Code/build_agent/Dockerfile'